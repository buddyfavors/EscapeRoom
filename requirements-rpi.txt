# Raspberry Pi — use a venv so you do not mix pip with Debian "externally managed" system Python.
#
#   sudo apt update
#   sudo apt install -y python3-pip python3-venv python3-dev build-essential
#   cd /path/to/KnottyBytes.EscapeRoom
#   python3 -m venv .venv
#   source .venv/bin/activate
#   python -m pip install -U pip setuptools wheel
#   pip install "MarkupSafe>=2.1.3"
#   pip install -r requirements-rpi.txt
#
# If pip still errors on MarkupSafe, try only wheels (no source build):
#   pip install --only-binary=:all: -r requirements-rpi.txt
#
# Optional: system evdev module instead of pip building evdev:
#   sudo apt install -y python3-evdev
#   (then you can use requirements.txt only and skip the evdev line below if desired)

-r requirements.txt
evdev>=1.7,<2
