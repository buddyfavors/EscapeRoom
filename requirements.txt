# Python 3.9+ (3.11+ optional). Pi OS often ships 3.9–3.10 — avoid 3.11-only APIs in app code.
# MarkupSafe first: avoids resolver / metadata issues on some ARM images (e.g. KeyError: markupsafe).
MarkupSafe>=2.1.3,<4
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
jinja2>=3.1.3,<4
pydantic>=2.6,<3
eval-type-backport>=0.2.0,<1
python-multipart>=0.0.9,<1
